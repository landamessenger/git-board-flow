export class Tokens {
    token: string;
    classicToken: string;
    constructor(
        token: string,
        classicToken: string,
    ) {
        this.token = token;
        this.classicToken = classicToken;
    }
}