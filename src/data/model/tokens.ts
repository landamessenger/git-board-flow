export class Tokens {
    token: string;
    tokenPat: string;

    constructor(
        token: string,
        tokenPat: string,
    ) {
        this.token = token;
        this.tokenPat = tokenPat;
    }
}